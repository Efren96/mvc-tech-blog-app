{{!-- <div>
    <div>
        <h4><a href="/post/{{id}}">{{title}}</a></h4>
        <p>Posted by {{user.username}} on {{format_date created_at}}</p>
    </div>
    <p>{{content}}</p>
</div> --}}

<div class="card">
  <div class="card-header">
     <h4><a href="/post/{{id}}">{{title}}</a></h4>
  </div>
  <div class="card-body">
    <blockquote class="blockquote mb-0">
    <p>{{content}}</p>
      <footer class="blockquote-footer"><p>Posted by {{user.username}}</p> on <cite title="Source Title">{{format_date created_at}}</cite></footer>
    </blockquote>
  </div>
</div>